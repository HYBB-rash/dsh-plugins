# Pocket Archive

这是一个尚未正式开发的本地优先读书笔记工具。当前只保留了内存中的笔记模型和一个未完成的导出入口。

已确认的产品约束：

- 没有网络时也必须能够查看和导出全部笔记。
- 普通用户应能把导出结果带到另一台电脑继续使用。
- 预期最多约一万条笔记，每条可能包含标签和少量 Markdown 文本。
- 当前没有数据库、同步服务或账户系统。

尚未决定导出采用单个 JSON 文件、目录结构，还是先引入 SQLite。团队不希望为了未来可能出现的需求提前搭建完整数据平台。
