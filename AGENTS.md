# 项目规则

- 开始工作前，先阅读当前目录及更近作用域的 `AGENTS.md`；再按任务关键词检索根目录 `MEMORY.md`，只把命中的经验当作可借鉴线索，配置、运行状态和外部系统仍以本次现场核对为准。
- `AGENTS.md` 只保存长期、跨任务、会约束后续行为的规则；项目现状、历史实验、验收记录和可复用踩坑放入 `MEMORY.md` 或其对应的专项文档。
- 除非用户明确授权，保持改动最小，不覆盖或回退已有无关改动；先检查工作树，并按风险做相称的验证。
- 生产发布只能走 `./release/dsh`：从明确 Git commit 构建同一不可变镜像，并遵守其停机、快照、验证、用户验收和显式回滚边界。不得直接同步源码、修改线上 selector、远程安装依赖，也不得恢复或扩展已退役的旧 `deploy.sh` 流程。
- 涉及 Docker 开发、快照、发版、验收或回退时，先阅读 `release/README.md`；OpenClaw 始终在流程之外，不得改动。
- 开发和上线前测试只能使用生产快照的隔离副本，并隔离真实凭据、Telegram、cron 和其他外部写入；持久化状态必须在镜像外。
- 测试、传输和生产必须使用同一镜像身份；测试后不得重建镜像、修改容器代码或补装依赖。
- 获得停机许可后，必须停止全部 DSH 写入者、生成一致快照，并将上一镜像及其兼容的数据、配置和凭据保留到生产验收通过并执行 `accept`。
- 生产验收必须覆盖服务、数据和真实业务，不能只看容器或 HTTP。只有明确、可逆且不改变业务或数据语义的发版问题，才允许现场修复，最多三十分钟；其他或无法分类的问题一律停止线上试错并报告。回退须另获批准、恢复完整旧版边界并重新验收。项目优先简单和可恢复，不得为缩短停机跳过验证或擅自引入高可用架构。
- 每项任务结束前，如本次发现了可复用的失败原因、边界或验证陷阱，向根目录 `MEMORY.md` 追加一条简短、可检索、带日期的经验；没有新经验则不要凑条目。
- 用户当前任务、明确验收条件及更近目录的规则优先于这里和 `MEMORY.md`。
