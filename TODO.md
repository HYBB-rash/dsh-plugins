# 当前交付记录

- F01-T4：已实现同焦点、同 generation 的 C41 + C14/C15 rolling candidate 与 identical requalification；候选仍须通过既有 deterministic renderer、两个 reviewer 和 owner-authenticated C28，未扩大到普通 assistant、direct 或任意触发。
- F07-T2：已在 Telegram 冷启动、首个 claim/model call 前按结构化来源四分 new、pre-canonical focus、legacy route 与 expected-missing；已完整保全的 canonical state 仍由既有 owner 恢复，不进入四分；旧 route 只保留为由 direct-user 事实重新进入既有 F02/F01/F06–F09 的迁移历史，缺失应有 context-manager state 保守进入既有错误面。
- F06-R1A：已为“claim 删除完成但同 id 尚无 `user/message`/request/provider 开始证据”增加标准 splice-log 恢复；原消息按 target/order/id 重插后必须通过 flush 与 detached exact readback，专用 wake 只作机械驱动且不进模型。持久证明失败保持单个 live pending、provider=0；冷启动不主动通知，进程内下一次合法 managed turn 独立保全该 input 后，才一次性消费内存事故标记并复用 F07 失败呈现。已有请求证据明确留给 R1B。
