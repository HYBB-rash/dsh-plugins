# 开发日志：cron 线上修复验收

- 日期：2026-09-05
- 范围：基于新 main 重新验证 cron 执行工具与列表输出修复；herman.hermes 官方 npm 普通归档安装及真实微信任务验收。

## 目标

修复微信任务只重新排期、不执行本地脚本，以及 cron_list 输出校验失败的问题。此前本地候选记录保留在 `dev-log-2026-09-05-cron执行工具与列表输出.md`；本日志记录用户要求重新调查主线和线上环境之后的工作。

用户先要求开发服验证，随后明确回复“这个情况，你直接上线测吧。没关系，个人项目。我授权。”本轮据此执行线上备份、停机、安装、启动与一次现有任务重跑；没有合并或推送分支。

## 时间线

以下时间均为北京时间。

1. 12:47—12:58：重新读取 main 和线上现场。main 已变为 `91e353c`，生产 current 为 `1df54a038a47ca01fc3fa3f2c97b5a7fc47d2614fab434286cfe609b00296438`，实际使用官方 npm Harness `0.1.2-rc.1`，Web 直接监听 `0.0.0.0:3080`。旧 Docker、旧代理、旧 v2 会话记录均不能代表当前运行状态。
2. 当前微信会话的模型请求已有 SSH 等工具，但没有本地 bash 和文件读取工具；新 main 的 cron_list 仍返回未声明的 cwd。用新 main 和当前 SDK 在隔离 JobStore 重现相同输出合同错误。读取新 `session.jsonl.zstd`，避免把旧 `session.v2.jsonl.zstd` 当作最新会话。
3. 将任务分支变基到最新 `91e353c`，解决 npm 版本与经验文档冲突，保留 main 的精确依赖和 SessionPersistence 新 API。拆除本任务 worktree 原先指向主工作树的 node_modules 符号链接，在 nix develop 内执行 npm ci，建立独立依赖。
4. 三个插件构建、25 项脚本检查和插件依赖边界检查通过。用户授权线上验证后，通过正式打包上传脚本准备一批依赖；该批完整测试为 55 文件、1160 项通过，随后上传。
5. 13:07：确认旧监督进程及其子树后，只向监督进程发送 TERM；等待旧树全部退出且 3080 释放。保存完整状态备份（不含可重装 node_modules 和历史备份目录），记录任务账本、运行账本及两份凭据的 SHA-256。另在隔离副本折叠全部 50 个活跃任务及其运行记录，通过。
6. 13:11：目标安装成功，current 更新为 `d8bd2e3f722769c22c4593810e77991d6b0aa453eb7cadf13657f19bdf26759e`。安装前后任务、运行账本及凭据哈希一致。通过本批启动器启动单一 Web 树，监督 PID 3209319、Web PID 3209379，实际 cwd 为 `/home/herman/.dsh/workspace`。
7. 13:12：loopback 和 LAN 的未认证 session API 为 401；域名直接 API 和未配对 remote 为 403。通过原有 token/Cookie 登录取得会话列表，向空闲的 Telegram 根会话提交验收要求：先 cron_list，再仅一次 cron_run_now 指定已有 `cron-241d3a1a`，禁止新建、删除、修改任务。
8. 13:13—13:14：真实模型调用 cron_list 成功，未再出现输出校验错误；cron_run_now 接受精确任务。新微信执行会话 request/header 已包含 bash/read 等工具。
9. 13:14:27—13:15:06：微信任务通过 bash 实际执行指定脚本，工具结果无错误。读取完整 3129 字符输出，含近三天背景、当前窗口事件和精华消息三个区块。模型生成 616 字符报告，13:15:05 任务成功结束，13:15:06.183 Telegram delivery 返回 delivered。
10. 13:16—13:17：比对报告与脚本输出，3 条星标摘录全部逐字一致，2 条公众号标题及 URL 均来自原文，报告以时钟行开头且含 4 个独立标题和无新消息回退。任务定义 SHA-256 在重跑后仍未改变。重新 fetch 并确认 main 与 origin/main 均为 91e353c，任务分支仍包含最新 main。

## 逻辑链条

主线迁移改变了运行入口和部分默认工具，但没有补上 scheduler 对本地执行预设的挂载。必须以当前请求中实际列出的工具判断能力，不能从“standard”标签或当前 npm 版本推断。修复继续通过本仓库调用官方 agentPresets.mount 完成，不改上游。

列表失败是本仓库输出声明与返回值不一致，补充可选 cwd 即可。无需更改现有任务数据。验证使用现有一次性任务的手动运行入口，不另建微信任务，不改变其排期。

## 改动

沿用已有候选的 manager 输出 schema、scheduler 可选 agentPreset 和 portable standard 配置；本轮将 dsh-agent-presets 依赖与 lock 对齐 main 的精确 `0.1.2-rc.1`。测试覆盖带/不带 cwd、新建/恢复/per_run 执行，以及显式 provider 环境不受普通预设影响。

## 验证

- 新 main 基线下构建、25 项脚本检查、依赖边界和 1160 项完整测试通过。
- 隔离副本的 50 个任务与运行账本折叠成功。
- 线上安装前后任务、运行账本与凭据哈希一致；服务恢复后入口认证矩阵符合当前部署合同。
- 真实 cron_list 已成功；微信执行会话已具备本地 bash/read。
- 真实任务只发生一次手动 claim：`manual:cron-241d3a1a:723d07927d771954949c7b8b8be2ad7b14e9a0bf3dcf249328045ba0048a9903`。bash 一次执行成功，最终正文和已投递 outputPreview 一致。
- 原始工具 stdout SHA-256：`df3cd4fb13fcfaf64cf52ac95db670e633f46b8fe857e46b8ba54c41aec9c5ea`。原文与报告内容留在目标机私有会话/日志中，仓库只保存校验结果。
- 3 条精华逐字保留；2 条公众号链接有原文依据；单聊、群聊、公众号、QQ 四个独立区块齐全，最终正文没有排期说明或前言。Telegram 已返回 delivered，用户实际阅读验收仍待确认。
- 本轮没有新做设备配对和真实 Notion 写入验收，不把入口 HTTP 与已有后台成功状态当作这两项业务证明。

## 遗留

线上存在历史递归生成的多个微信任务，本轮未批量删除。自动化检查不代替用户对 Telegram 报告的阅读验收。

本轮备份：`/home/herman/.local/share/dsh-web-backups/hermes-20260905-cron-tools/dsh-state.tar.gz`。当前普通归档流程没有自动 accept 或 rollback；备份不代表已执行回退演练。
